import Home from './Home';

export const HomeConfig = {
    settings: {
        layout: {
            config: {}
        }
    },
    routes  : [
        {
            path     : '/',
            component: Home
        }
    ]
};